# Development override: publishes PostgreSQL on localhost so the services can
# run outside Docker (`make dev`). Not for production.
services:
  db:
    ports:
      - "127.0.0.1:${OBRABI_DEV_DB_PORT:-5432}:5432"
    # Ports cannot be published on the internal-only network.
    networks: [backend, edge]
